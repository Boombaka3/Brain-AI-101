import DeepLearningBridge from './DeepLearningBridge'

function BigPicture() {
  return (
    <section className="m3-section">
      <div className="m3-section-card m3-backprop-callout">
        <strong>Feedback during inference</strong>
        The network structure stays the same after training. During inference, information moves forward through the trained layers to produce an output.
      </div>
    </section>
  )
}

export default BigPicture
